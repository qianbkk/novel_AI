"""config/prompt_templates.py

Externalized prompt config: 7 hook types / 7 shuang types / 3 genre writing
instructions / universal writing rules. All agents import from here —
no hardcoded prompts inside agents/ (per CLAUDE.md rule).

Migrated 1:1 from novel_AI/config/prompt_templates.py.
"""
from __future__ import annotations


# ═══════════════════════════════════════════════
# 七种章末钩子定义（V3方案7.4节）
# ═══════════════════════════════════════════════
HOOK_TYPES = {
    "悬念钩": {
        "desc": "主角或读者获知了一个关键信息的存在，但内容尚未揭开",
        "pattern": "结尾揭示「有某件事/某个人」存在，但不说是什么",
        "example": "他突然发现账户里多了一笔钱——打款人的名字让他愣在原地。",
        "strength": "中",
        "best_for": ["铺垫", "发展"],
    },
    "危机钩": {
        "desc": "明确的威胁或危险正在逼近，读者知道下章必须解决",
        "pattern": "结尾出现直接威胁，且威胁在下章必须面对",
        "example": "手机震动。屏幕上是系统警报：【检测到三级债崩，临江市××街道，倒计时12分钟。】",
        "strength": "高",
        "best_for": ["发展", "弧高潮前一章"],
    },
    "信息钩": {
        "desc": "揭露一个改变读者对局面理解的新信息",
        "pattern": "结尾说出一句让读者「原来如此/不可能吧」的话",
        "example": "贺苗把那份档案推到他面前。「你父亲，不是旁观者。」",
        "strength": "高",
        "best_for": ["发展", "爽点后"],
    },
    "情感钩": {
        "desc": "角色关系到达一个节点，读者想知道接下来怎样",
        "pattern": "某段关系（爱情/友情/对立）在结尾发生明确变化或到达临界点",
        "example": "苏云溪在门口停住脚。「你刚才帮我，是因为委托，还是别的原因？」\n陆承没有回答。",
        "strength": "中",
        "best_for": ["发展", "过渡"],
    },
    "反转钩": {
        "desc": "读者以为的结果被推翻，引发重新理解全局的冲动",
        "pattern": "结尾一句话让前文某个「已定论」的事情变得不确定",
        "example": "直到离开那栋楼，陆承才意识到：章廷全程说的，都是假的。",
        "strength": "极高",
        "best_for": ["爽点", "弧高潮"],
    },
    "升级钩": {
        "desc": "主角获得新能力/晋升/资源，读者想看他用这个做什么",
        "pattern": "结尾完成某个升级，但立刻暗示下一个更大的挑战已在等待",
        "example": "【境界提升：感债者 → 识债者】\n系统提示音刚落，陆承就看到了之前完全看不到的东西——整座城市上空，有一张几乎覆盖所有人的巨大灰色网。",
        "strength": "高",
        "best_for": ["爽点", "弧高潮"],
    },
    "对抗钩": {
        "desc": "下一章的正面冲突已经摆在台面上，读者在等开打",
        "pattern": "结尾明确宣告：两方将在下章正面交锋",
        "example": "章廷把执法令牌放在桌上。「明天上午十点，债主委员会。你最好到场。」",
        "strength": "高",
        "best_for": ["铺垫末", "弧高潮前"],
    },
}


# ═══════════════════════════════════════════════
# 爽点类型定义
# ═══════════════════════════════════════════════
SHUANG_TYPES = {
    "打脸":   "主角揭穿对方的把戏或证明对方错误，使其在众人面前难堪",
    "升级":   "主角获得新能力/新资源/境界突破，立刻用于解决眼前问题",
    "逆袭":   "主角在明显劣势下以智取胜，翻盘局面",
    "揭秘":   "关键信息/秘密被揭开，读者和主角同时获得「原来如此」的满足感",
    "报复":   "之前针对主角的人受到了应有的惩罚（主角直接或间接导致）",
    "碾压":   "主角对能力/地位低于自己的挑衅者展现实力差距",
    "救场":   "主角在最后关头出现，解决他人无法应对的危机",
}


# ═══════════════════════════════════════════════
# 情绪核心类型定义（2026-07-25 战略审视 Commit 3）
# 来源：《告别单机》§8 情绪锚点 + 《告别"自嗨"》§1 网文是情绪消费品
# 设计：每章必填 emotion_core（7 类）+ emotion_intensity（1-5）。
# 重要约束：避免连续 3 章同一 emotion_core 让读者情绪疲劳弃书
# （由 acceptance/beat_checker 阶段校验）。
# ═══════════════════════════════════════════════
EMOTION_CORES = {
    "憋屈":   "主角被压制、冤屈、愤怒蓄势（高密度小爽点前常铺垫）",
    "压抑":   "气氛低沉、命运压迫、危机逼近（不蓄不宣泄）",
    "爽快":   "爽点直接释放（扮猪吃虎摊牌、打脸、反杀）",
    "震惊":   "读者和主角同时认知冲击（身份揭露 / 反转 / 世界真相）",
    "虐心":   "情感深度拉扯（生离死别 / 爱恨两难 / 牺牲）",
    "甜蜜":   "情感正向升温（撒糖 / 心动 / 关系确认）",
    "燃":     "战斗/奋斗激昂（热血冲锋 / 逆袭反杀 / 决战）",
}


# ═══════════════════════════════════════════════
# 三题材专属 Writer 写作指令（V3方案7.3节）
# ═══════════════════════════════════════════════
GENRE_WRITING_INSTRUCTIONS = {
    "都市": """
【都市爽文核心写作指令】
节奏规则：
- 每500字必须有一个「信息密度峰值」（新信息 / 新冲突 / 新情绪）
- 对话是节奏的调速器：紧张时短句对话，缓和时可有描写穿插
- 不允许连续3段以上的纯心理描写，心理必须通过行动/对话外化

都市特有要求：
- 场景必须「可感知」：不说「走进一栋大楼」，说「推开旋转门，前台正打电话，声音被走廊的大理石地面弹得很响」
- 金钱/权力必须具体：不说「一笔大钱」，说「380万，一次性转账」
- 反派/对立人物必须有逻辑：他们为什么这样做，要站得住脚

系统流特有要求：
- 系统提示每章出现2-4次，不多不少
- 数字要有节奏感：【人情点+150】比【人情点+149】更有冲击力
- 系统「说话」要简洁：不超过15字，禁止解释性系统提示

禁止写法：
- 「陆承心想，这个人果然不简单」（直接告诉读者结论）
- 连续两段开头都用人名
- 「话音刚落」「此话一出」等戏剧腔过渡词
""",

    "玄幻": """
【玄幻修仙核心写作指令】
世界感营造：
- 每章至少一处「世界细节」展示：一个普通人路过时的反应 / 一个规则的具体体现
- 境界对比要有画面感：「凝气期对元婴期，就像蜡烛对太阳」

战斗要求：
- 战斗前必须有「战力分析」（主角视角，内心评估）
- 战斗中每隔300字切换一次主被动
- 战斗结尾必须有「代价」：受伤 / 暴露信息 / 消耗资源

修炼要求：
- 突破场景：外部变化（天地异象）+ 内部变化（感受描写）+ 意外干扰（必须有）
""",

    "萌宝甜宠": """
【萌宝甜宠核心写作指令】
萌点设计：
- 萌宝台词：用4-8字短句，偶尔故意说错词（「大坏蛋」「哼哼」），每章至少2处萌点
- 萌宝视角：偶尔切换成孩子视角，用最直接的语言描述复杂的大人关系

甜点设计：
- 每章至少一处「心跳」场景：眼神 / 无意接触 / 一句话说进心里
- 男主行动>语言：用行动展示在意，不要直接说「他爱上了她」

家庭温情：
- 吃饭场景是甜宠文的天然蓄水池，用好三人同桌的画面
""",
}


# ═══════════════════════════════════════════════
# 通用写作铁律（所有题材都适用）
# ═══════════════════════════════════════════════
UNIVERSAL_WRITING_RULES = """
【通用写作铁律（所有章节必须遵守）】

格式规则：
1. 每段不超过120字，对话段不超过60字
2. 对话占比≥35%（字数比）
3. 章节结尾最后一段必须是强钩子，绝对禁止以「感悟/总结/决心」收尾

禁用词汇（AI腔高风险词）：
此刻、蓦然、不禁、心中一动、深吸一口气、不由得、莫名、涌上心头
眼眸、嘴角微扬、眸子、低沉的声音、沉声道、缓缓、悄然、骤然
凝视、喃喃、霎时、不料、倏地、话音刚落、此话一出

禁用句式：
- 「X想到这里，Y」（过渡性总结句）
- 连续两个「X说：」「Y道：」开头的段落
- 「正因如此」「话虽如此」「说到底」等AI逻辑连接词
- 排比式心理独白（「他知道...他明白...他清楚...」）

系统提示格式（系统流题材）：
- 使用【】标注，内容不超过15字
- 每章出现2-4次
- 禁止解释性系统提示（「系统检测到您的行为符合诚信原则」= 禁止）
"""


# ═══════════════════════════════════════════════
# 4 招方法论内化（2026-07-25 战略审视 Commit 0）
# 来源：write/《写小说平淡没看点怎么办》/《网文3层期待感设计法》/
#      《所有爆款小说背后都有这4大共性》/《写小说拒绝平铺直叙：手把手教你三线并行的叙事结构》
# 验证：docs/wiki/03-Writing-Engine.md §已知方法论 gap 与补全计划 §4
# ═══════════════════════════════════════════════

# 信息差（Info Asymmetry）— 3 模式写作执行清单
# 让读者爽的核心是"读者知道的比角色多"，或"角色知道但周围人不知道"
INFO_ASYMMETRY_INSTRUCTION = """
【信息差三模式执行清单】（每章必检 ≥1 项）

1. 读者知/主角不知（危机钩）：
   - 章内某关键信息只有读者看见（反派密谋 / 主角被算计）
   - 主角却浑然不知，按"正常"逻辑行动
   - 读者一边看主角"傻乎乎"一边在心里喊"小心！"
   - 操作：开头 500 字内埋设"读者已知威胁"，让主角在其后 1500 字里走向危机

2. 主角知/配角不知（爽点预判）：
   - 主角拥有某种底牌（满级装萌新 / 系统暗藏 / 真实身份）
   - 周围反派疯狂挑衅、路人摇头叹息
   - 读者（已知底牌）看着反派像小丑一样跳，期待掀牌时刻
   - 操作：让反派/路人连续 ≥3 次嘲讽/质疑作为"压抑层"，章末才露一手

3. 双方均不知（悬念双盲）：
   - 主角和反派都不知道的第三因素即将介入（天道 / 系统升级 / 第三方势力）
   - 双方按各自逻辑博弈，读者也不知道结果
   - 用于伏笔回收章 / 转折高潮章
   - 操作：章内用"不可名状的预感"渲染氛围，章末以"某处，X 睁开眼"收束

⚠ 避免：信息差全章空转（读者既不知道也不好奇）、伪信息差（实际主角一眼看穿）
"""

# 但是法则（But Law）— 替换"然后"为"但是"的章节骨架
# 张力的本质是目标与阻碍的对抗，不是事件流水账
BUT_LAW_INSTRUCTION = """
【但是法则执行清单】（每章必检 ≥1 个但是转折）

章内"然后→但是"强制改造清单：
- 章首 200 字内：明确本章主角**目标**（不是任务，是内心想要的东西）
- 章中 1/3 处：第一个"但是"——**阻碍 1**（物理/情感/信息/道德四类至少其一）
- 章中 2/3 处：第二个"但是"——**阻碍 2**（必须比阻碍 1 更难，更出乎意料）
- 章尾前 300 字：第三个"但是"——**反转 / 升级 / 钩子**

障碍四类必须轮换或叠加（避免重复类型）：
- 物理：资源/身体/技能不够
- 情感：爱恨/愧疚/亲情冲突
- 信息：被误导 / 关键情报缺失 / 误判对手实力
- 道德：救一人 vs 救百人 / 说谎 vs 失信 / 复仇 vs 原谅

如果某章只有一个"但是"——本章张力不够，必须重写或合并到下一章。
如果某章全是"然后"——本章为废章，需重排章节。
"""

# 三层期待感（Three-Layer Hook）— 微观/中观/宏观钩子设计
# 微观留钩子 = 当章停不下来；中观逗猫棒 = 追读 100 章；宏观天坑 = 留读者 3 年
THREE_LAYER_HOOK_INSTRUCTION = """
【三层期待感执行清单】（每章必检 3 项全填）

1. 微观期待（当章末钩子）：
   - 章末最后 200 字必有一个"未完成的悬念"（信息差闭合前 1 秒 / 高潮触发前 0.5 秒 / 反派底牌刚摊开 1/3）
   - 严禁以「感悟/总结/决心」收尾
   - 章末钩子类型必须 ≥1 个 [悬念/危机/信息/情感/反转/升级/对抗] 钩之一

2. 中观期待（阶段性大奖）：
   - 本章必须**至少**推动一个 5-30 章内会兑现的承诺（中期对决 / 身份揭露 / 主角觉醒 / 大反派出场）
   - 哪怕本章不写兑现，也要在对白/动作里**强化读者对该承诺的期待**（提及名字 / 暗示进度 / 倒计时）
   - 严禁让承诺消失在细节里

3. 宏观期待（终极悬念）：
   - 如果本书有终极悬念（"父亲的遗物里到底有什么" / "系统的真正主人是谁"），本章必须**至少暗示一次**
   - 暗示方式：主角的内心独白 / 配角的闲谈 / 环境的细节呼应（不可过于频繁）
   - 终极悬念每隔 ≥5 章才可再提一次（避免疲劳）
"""

# 模块化叙事（Modular Narrative）— 主线/支线/暗线交织
# 三线并行不是真并行执行，而是章与章之间"切换主角镜头"的隐性编织
MODULAR_NARRATIVE_INSTRUCTION = """
【模块化叙事执行清单】（每章必检 三线锚点）

每章承担三类叙事模块之一或多个（可叠加，但主线模块必占 60%+）：
- **主线模块**：推进主角最高目标（晋级 / 复仇 / 解谜）
- **支线模块**：丰富人物或世界观（配角登场 / 新地图 / 情感戏）
- **暗线模块**：埋设/回收伏笔（看似无关的细节，实际为后期反转准备）

章内操作三原则：
1. 锚点归一：不论本章走哪条线，最终必须**指向同一个主线结局**
   （朝堂斗争 / 边境厮杀 / 底层挣扎都服务于"推翻暴政"这一终极）
2. 高潮切断：当 A 线推到高峰（推开密室 / 反派真相浮出），立刻切 B 线
   利用读者生理性好奇强迫读完 B 线
3. 漏斗汇聚：每 5-10 章让两条线发生一次**实际交叉**
   （主角打怪时反派正好赶来 / 两条支线的配角原来是同一人）

⚠ 反模式：每章只走单线、支线完全游离于主线、暗线埋了不收
"""


# ═══════════════════════════════════════════════
# 角色口癖模板（从设定包提取后在这里使用）
# ═══════════════════════════════════════════════
CHARACTER_VOICE_REMINDER_TEMPLATE = """
【角色口癖提醒（本章必须体现）】
{character_voices}

注意：口癖不是每段都用，而是在「角色情绪密度高」的时刻自然出现（每人每章1-2次）。
"""


def get_character_voice_reminder(characters: list, setting: dict) -> str:
    """从设定包提取角色口癖，生成提醒文本"""
    key_chars = {c["name"]: c for c in setting.get("key_characters", [])}
    mc = setting.get("protagonist", {})

    voice_lines = []
    # 主角
    mc_name = mc.get("name", "")
    if mc_name in characters and mc.get("speech_quirks"):
        quirks = " / ".join(mc["speech_quirks"][:2])
        voice_lines.append(f"{mc_name}：{quirks}")

    # 配角
    for char_name in characters:
        if char_name in key_chars:
            quirks = key_chars[char_name].get("speech_quirks", [])
            if quirks:
                voice_lines.append(f"{char_name}：{' / '.join(quirks[:1])}")

    if not voice_lines:
        return ""
    return CHARACTER_VOICE_REMINDER_TEMPLATE.format(
        character_voices="\n".join(voice_lines)
    )


def get_hook_guidance(hook_type: str) -> str:
    """获取指定钩子类型的写法指导"""
    hook = HOOK_TYPES.get(hook_type, {})
    if not hook:
        return f"结尾类型：{hook_type}"
    return (
        f"结尾钩子类型：{hook_type}\n"
        f"写法要点：{hook['pattern']}\n"
        f"参考示例：{hook['example']}"
    )


# 4 招方法论常量化（避免在 writer.py 拼字符串时拼错）
_METHODOLOGY_BLOCKS = {
    "info_asymmetry": INFO_ASYMMETRY_INSTRUCTION,
    "but_law": BUT_LAW_INSTRUCTION,
    "three_layer_hook": THREE_LAYER_HOOK_INSTRUCTION,
    "modular_narrative": MODULAR_NARRATIVE_INSTRUCTION,
}


def get_methodology_instruction(aspects: list[str] | None = None) -> str:
    """获取 4 招方法论拼装后的指令块。

    用法:
        get_methodology_instruction()                       # 全部 4 招
        get_methodology_instruction(["info_asymmetry"])      # 仅信息差
        get_methodology_instruction(["but_law", "three_layer_hook"])

    设计原则（2026-07-25 战略审视 Commit 0）:
    - 4 招默认全开（每章都有这 4 项约束），保持 writer prompt 长度可控（~4KB 总和）
    - 调用方可以按章节任务类型裁剪（如 plot_engineer 章节关闭 but_law 强调 modular_narrative）
    - 默认拼接顺序：但 → 信息差 → 期待感 → 模块化（从微观到宏观）
    - 防御性去重：同一 aspect 重复传入时只输出一次（避免 LLM prompt 重复指令干扰）
    """
    if aspects is None:
        aspects = ["but_law", "info_asymmetry", "three_layer_hook", "modular_narrative"]
    parts = []
    seen: set[str] = set()
    for k in aspects:
        if k in seen:
            continue  # 去重
        seen.add(k)
        v = _METHODOLOGY_BLOCKS.get(k)
        if v:
            parts.append(v.strip())
    if not parts:
        return ""
    header = "【方法论执行清单（每章写作前过一遍）】"
    return header + "\n\n" + "\n\n".join(parts)


def get_genre_instruction(genre: str) -> str:
    """根据题材获取专属写作指令"""
    for key, instruction in GENRE_WRITING_INSTRUCTIONS.items():
        if key in genre:
            return instruction
    return GENRE_WRITING_INSTRUCTIONS["都市"]  # 默认都市