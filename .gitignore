# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/
.vite/
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Local env / secrets
.env
*.env
!.env.example
!.env.template

# Local data and runtime output
backend/data/

# Personal tooling scratch dirs (本地自查，不入库)
.claude-viz/
.understand-anything/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Playwright test artifacts
.playwright-cli/
test_output/
.test_project_id
frontend/audit_screenshots/

# Local benchmark / generated engine artifacts
backend/setting_package.json
docs/final-verification-report.txt

# dev.bat runtime output (port logs, launcher .cmd)
.runlogs/
PID

# Backend runtime logs (see backend/app/logging_setup.py)
backend/logs/

# Dev master key for Fernet encryption (迭代 #82)
# 由 app/security.py 在 MASTER_KEY env 未设时自动生成持久化——
# dev.bat --reload 工作流下保证重启后仍能解密 Provider key。
# （已在 backend/data/ 规则覆盖，但显式列出防止后续重构挪动 data 目录时漏掉）
backend/data/.dev_master_key
*.dev_master_key
